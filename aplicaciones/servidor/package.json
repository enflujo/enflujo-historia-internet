{
  "name": "servidor",
  "scripts": {
    "prod": "docker compose up -d",
    "dev": "docker compose -f docker-compose-desarrollo.yml up -d",
    "dev:logs": "docker compose up",
    "apagar": "docker compose down"
  }
}
