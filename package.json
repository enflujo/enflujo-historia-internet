{
  "name": "enflujo-historia-internet",
  "private": true,
  "packageManager": "yarn@4.9.2",
  "workspaces": [
    "aplicaciones/*"
  ],
  "scripts": {
    "dev": "yarn workspace www dev",
    "servidor": "yarn workspace servidor prod",
    "servidor:dev": "yarn workspace servidor dev",
    "servidor:logs": "yarn workspace servidor dev:logs",
    "servidor:apagar": "yarn workspace servidor apagar",
    "build": "yarn workspace www build",
    "lint": "prettier --check .",
    "lint:fix": "prettier --write --list-different .",
    "preview": "yarn workspace www preview",
    "revisar": "yarn workspace www revisar"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
